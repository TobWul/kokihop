import Firebase from "./firebase";
import { FirebaseContextProvider, FirebaseContextConsumer } from "./context";

export default Firebase;
export { FirebaseContextProvider, FirebaseContextConsumer };
